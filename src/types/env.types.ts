export interface Users {
    standard_user: string;
    locked_out_user: string;
    problem_user: string;
    performance_glitch_user: string;
    error_user: string;
    visual_user: string;
    password: string;
}